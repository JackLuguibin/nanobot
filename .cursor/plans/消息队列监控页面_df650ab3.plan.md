---
name: 消息队列监控页面
overview: 新增消息队列监控页面，后端扩展 ZMQ Bus 和 MessageBus 状态接口，新建队列 API 路由和 WebSocket 推送，前端新增 Queue 页面实时展示 Channel 层 (asyncio.Queue inbound/outbound) 和 Agent 层 (ZeroMQ PUB/ROUTER/SUB) 的状态。
todos:
  - id: ext-zmq-bus
    content: 扩展 zmq_bus.py 添加 get_queue_status 方法
    status: completed
  - id: ext-state
    content: 扩展 state.py BotState 添加 get_queue_status 方法
    status: completed
  - id: new-queue-api
    content: 新建 queue.py API 路由
    status: completed
  - id: reg-queue-route
    content: 注册 queue_router 到 routes.py
    status: completed
  - id: ws-message-type
    content: models/base.py 添加 QUEUE_UPDATE 消息类型
    status: completed
  - id: ws-queue-broadcast
    content: WebSocket 添加 broadcast_queue_update 方法
    status: completed
  - id: fe-types
    content: 新建前端类型定义 types_queue.ts
    status: completed
  - id: fe-api
    content: 新建前端 API 调用 queue.ts
    status: completed
  - id: fe-page
    content: 新建前端页面 Queue.tsx
    status: completed
  - id: fe-route
    content: 注册路由到 App.tsx
    status: completed
  - id: fe-sidebar
    content: 添加侧边栏菜单入口到 Layout.tsx
    status: completed
isProject: false
---

## 整体架构

```
前端 (React)
    │
    ▼
后端 API (console/server/api/queue.py)  [新建]
    │
    ▼
Extension 层 (zmq_bus.py + state.py)
    │
    ▼
nanobot 核心 (MessageBus.inbound/outbound, ZeroMQBus)
```

## 实现步骤

### 阶段 1: 后端

#### 1.1 扩展 `console/server/extension/zmq_bus.py`

在 `ZeroMQBus` 类末尾添加以下方法：

```python
def get_queue_status(self) -> dict:
    """获取 ZMQ Bus 的连接状态和统计信息。"""
    return {
        "is_initialized": self._is_initialized,
        "bind_addr": self._bind_addr,
        "pub_socket": {
            "type": "PUB",
            "address": self._bind_addr,
            "connected": self._pub_socket is not None,
        },
        "router_socket": {
            "type": "ROUTER",
            "address": f"tcp://127.0.0.1:{int(self._bind_addr.split(':')[-1]) + 1}",
            "connected": self._router_socket is not None,
        },
        "sub_sockets": [
            {
                "agent_id": full_id,
                "bot_id": full_id.split(":")[0],
                "topics": [],  # ZMQ 不直接暴露订阅主题，从 socket 选项读取较复杂，留空列表
                "address": self._bind_addr,
                "connected": socket is not None,
            }
            for full_id, (socket, _) in self._sub_sockets.items()
        ],
        "pending_delegations": len(self._pending_delegations),
    }
```

并在文件末尾添加模块级函数：

```python
def get_queue_status() -> dict:
    """模块级便捷函数，获取全局 ZMQ Bus 状态。"""
    bus = get_zmq_bus()
    if bus is None or not bus.is_initialized:
        return {"is_initialized": False}
    return bus.get_queue_status()
```

#### 1.2 扩展 `console/server/api/state.py`

在 `BotState` 中添加 `get_queue_status()` 方法（async），从 `_channel_manager.bus` 获取 `inbound_size`/`outbound_size`：

```python
async def get_queue_status(self) -> dict:
    """获取当前 Bot 的队列状态（Channel 层 + Agent 层）。"""
    # Channel 层：MessageBus inbound/outbound
    channel_queue = {
        "inbound_size": 0,
        "outbound_size": 0,
    }
    if self._channel_manager and hasattr(self._channel_manager, "bus"):
        bus = self._channel_manager.bus
        channel_queue = {
            "inbound_size": bus.inbound_size if hasattr(bus, "inbound_size") else 0,
            "outbound_size": bus.outbound_size if hasattr(bus, "outbound_size") else 0,
        }

    # Agent 层：ZeroMQ Bus
    from console.server.extension.zmq_bus import get_queue_status as get_zmq_status

    zmq_queue = get_zmq_status()

    return {
        "bot_id": self.bot_id,
        "channel_queue": channel_queue,
        "zmq_queue": zmq_queue,
        "last_updated": datetime.now().isoformat(),
    }
```

在 `BotStateManager` 中添加：

```python
async def get_all_queue_status(self) -> list[dict]:
    """汇总所有 Bot 的队列状态。"""
    results = []
    for bot_id, state in self._states.items():
        try:
            status = await state.get_queue_status()
            results.append(status)
        except Exception as e:
            results.append({"bot_id": bot_id, "error": str(e)})
    return results
```

#### 1.3 新建 `console/server/api/queue.py`

```python
"""消息队列监控 API。"""
from fastapi import APIRouter, Depends
from console.server.api.state import get_state_manager, BotStateManager

router = APIRouter()

@router.get("/status")
async def get_queue_status(bot_id: str | None = None):
    """获取所有 Bot（或指定 Bot）的队列状态。"""
    manager = get_state_manager()
    if bot_id:
        state = manager.get_state(bot_id)
        return await state.get_queue_status()
    return await manager.get_all_queue_status()
```

#### 1.4 注册路由 `console/server/api/routes.py`

在 imports 区域添加：

```python
from console.server.api.queue import router as queue_router
```

在 `router` 注册处添加：

```python
router.include_router(queue_router, prefix="/queue", tags=["queue"])
```

#### 1.5 WebSocket 广播 `console/server/api/websocket.py`

在 `ConnectionManager` 中添加方法：

```python
async def broadcast_queue_update(self, data: dict) -> None:
    """广播队列状态更新。"""
    from console.server.models.base import WSMessageType
    message = WSMessage(
        type=WSMessageType.QUEUE_UPDATE,
        data=data,
    )
    await self.send_message(message)
```

在 `console/server/models/base.py` 的 `WSMessageType` enum 中添加：

```python
QUEUE_UPDATE = "queue_update"
```

---

### 阶段 2: 前端

#### 2.1 新建类型 `console/web/src/api/types_queue.ts`

```typescript
export interface ChannelQueueStatus {
  inbound_size: number;
  outbound_size: number;
}

export interface ZMQSocketInfo {
  type: string;
  address: string;
  connected: boolean;
}

export interface ZMQQueueStatus {
  is_initialized: boolean;
  bind_addr: string;
  pub_socket: ZMQSocketInfo | null;
  router_socket: ZMQSocketInfo | null;
  sub_sockets: Array<{
    agent_id: string;
    bot_id: string;
    topics: string[];
    address: string;
    connected: boolean;
  }>;
  pending_delegations: number;
}

export interface QueueStatus {
  bot_id: string;
  channel_queue: ChannelQueueStatus;
  zmq_queue: ZMQQueueStatus;
  last_updated: string;
  error?: string;
}
```

#### 2.2 新建 API `console/web/src/api/queue.ts`

```typescript
import { fetchJson } from './client';
import type { QueueStatus } from './types_queue';

export async function fetchQueueStatus(botId?: string): Promise<QueueStatus[]> {
  const params = botId ? `?bot_id=${encodeURIComponent(botId)}` : '';
  return fetchJson<QueueStatus[]>(`/api/queue/status${params}`);
}
```

#### 2.3 新建页面 `console/web/src/pages/Queue.tsx`

页面使用 Ant Design 组件，参考 Dashboard 的样式风格：

- 顶部 Header：标题 "消息队列监控"、刷新按钮、自动刷新开关（默认开启，3秒间隔）
- 逐个 Bot 展示：
  - Channel 层 Card：`Statistic` 组件展示 `inbound_size` 和 `outbound_size`，带颜色指示（空=绿色，有消息=橙色/红色）
  - Agent 层 Card：展示 PUB/ROUTER Socket 连接状态（Table 展示 sub_sockets 列表）
- 使用 `useQuery` 配合 `refetchInterval` 实现自动刷新
- WebSocket 订阅 `queue_update` 消息触发 refetch（复用 `useWebSocket` hook）

#### 2.4 注册路由 `console/web/src/App.tsx`

```tsx
import Queue from './pages/Queue';

// 在 <Routes> 中添加
<Route path="/queue" element={<Queue />} />
```

#### 2.5 添加侧边栏入口 `console/web/src/components/Layout.tsx`

在 `Control` section（位于 `/health` 和 `/activity` 之间）添加：

```tsx
{ path: '/queue', label: 'Queue', icon: QueueIcon }
```

需要从 lucide-react 导入 `Queue` 图标（如果不存在则用 `BarChart2` 或 `Activity`）。

---

## 关键文件清单


| 操作  | 文件                                      |
| --- | --------------------------------------- |
| 修改  | `console/server/extension/zmq_bus.py`   |
| 修改  | `console/server/api/state.py`           |
| 新建  | `console/server/api/queue.py`           |
| 修改  | `console/server/api/routes.py`          |
| 修改  | `console/server/api/websocket.py`       |
| 修改  | `console/server/models/base.py`         |
| 新建  | `console/web/src/api/types_queue.ts`    |
| 新建  | `console/web/src/api/queue.ts`          |
| 新建  | `console/web/src/pages/Queue.tsx`       |
| 修改  | `console/web/src/App.tsx`               |
| 修改  | `console/web/src/components/Layout.tsx` |


