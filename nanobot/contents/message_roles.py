"""Canonical chat message role strings (OpenAI-style transcript roles)."""


class ROLES:
    """Namespace for standard LLM transcript roles."""

    SYSTEM = "system"
    USER = "user"
    ASSISTANT = "assistant"
    TOOL = "tool"
